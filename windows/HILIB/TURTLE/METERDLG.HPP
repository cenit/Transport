///////////////////////////////////////////////////////////////////////////////
////////////////////////// MeterDialog class definition ///////////////////////
///////////////////////////////////////////////////////////////////////////////

class MeterDialog : public ModelessDialog
{
     HWND hPar;
     AmpMeter *opMeter;
   public:
     MeterDialog(int ResID, Window *opParent, HWND hPar2);
     ~MeterDialog(VOID);
     BOOL Init( void);
     VOID SetValue(int);
};

//////////////////////////////////// EOF ///////////////////////////////////
